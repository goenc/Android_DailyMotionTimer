最大回数の管理をカウント画面用と上下設定用で分離した。

変更内容:
・カウント画面の最大回数を専用状態として保持するように変更。
・設定画面の最大回数は上下タイマー専用のまま維持。
・カウント画面の表示、完了文言、進捗色を専用の最大回数に統一。
・設定画面の最大回数表示は従来どおり上下専用の値を参照するように整理。

確認:
・.\gradlew.bat :app:assembleDebug 成功。
