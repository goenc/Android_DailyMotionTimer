11以上のカウント音声を、TTSではなく既存の1から10の音声素材を使った単一ファイル合成へ変更した。

変更内容:
・11以上はライブTTS再生をやめ、1から10のwav素材から回数ごとのwavを生成して再生する方式へ変更。
・各回数は1ファイルとして扱い、複数ファイルの逐次再生にはしない構成に整理。
・素材音声の前後無音を削り、つなぎ目にクロスフェードを入れて二桁発音の不自然さを抑制。
・1から10とフェーズ音声の既存挙動は維持。

確認:
・.\gradlew.bat :app:assembleDebug 成功。
