上下カウントが、画面上に表示されている最大回数まで繰り返すように修正した。

変更内容:
・上下モードの継続条件を `loopEnabled` 依存から最大回数依存へ変更。
・現在のラウンド数が最大回数に達するまで、Fast/Slow の往復を続けるようにした。
・完了時は表示中の最大回数で終了するように維持した。

確認:
・.\gradlew.bat :app:assembleDebug 成功。
