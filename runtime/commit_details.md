通常カウントタブを追加した。

変更内容:
・TimerModeを追加し、上下タイマーと通常カウントを切り替え可能にした。
・通常カウントは準備10秒後に1秒ごとに加算し、最大回数で完了する。
・動作中と一時停止中はタブ切り替えを無効化した。
・選択中のタイマーモードをDataStoreに保存するようにした。
・11回以上の音声カウントをTTSで読み上げるようにした。
・既存の未コミットロード画面変更と参照画像を現在状態として含めた。

確認:
・.\gradlew.bat :app:assembleDebug 成功。
